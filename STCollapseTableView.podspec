Pod::Spec.new do |s|

    s.name = 'STCollapseTableView'
    s.version = '0.1.1'
    s.platform = :ios, '7.0'
    s.license = 'MIT'
    s.homepage = 'https://github.com/iSofTom/STCollapseTableView'
    s.author = { 'Thomas Dupont' => '' }
    s.source = { :git => 'https://github.com/iSofTom/STCollapseTableView.git, :tag => s.version }
    s.summary = ''
    s.description = ':)'

    s.requires_arc = true

    s.source_files = 'STCollapseTableView/*.{h,m}'

end
